<section id="change-password-wrapper">
  <h2>
    {{t "ui.change_password"}}
  </h2>
  <p>{{t "ui.enter_your_current_password_and_a_new_password_to_change_it"}}</p>
  <form 
    id="change-password"
    hx-post="/api/auth/change-password"
    hx-select="form"
    hx-swap="outerHTML"
    hx-sync="this:abort"
  >
    <div class="inputs">
      <label class="{{#if errors.currentpassword}}error{{/if}}">
        {{t "ui.current_password"}}
        <input 
          id="currentpassword" 
          name="currentpassword" 
          type="password" 
          placeholder="{{t 'ui.current_password_2'}}"
          hx-preserve="true"
        />
        {{#if errors.currentpassword}}<p class="error">{{errors.currentpassword}}</p>{{/if}}
      </label>
      <label class="{{#if errors.newpassword}}error{{/if}}">
        {{t "ui.new_password"}}
        <input 
          id="newpassword" 
          name="newpassword" 
          type="password" 
          placeholder="{{t 'ui.new_password_2'}}"
          hx-preserve="true"
          autocomplete="new-password"
        />
        {{#if errors.newpassword}}<p class="error">{{errors.newpassword}}</p>{{/if}}
      </label>
    </div>
    <button type="submit" class="primary">
      <span>{{> icons/reload}}</span>
      <span>{{> icons/spinner}}</span>
      {{t "ui.update"}}
    </button>
    {{#if error}}
      {{#unless errors}}
        <p class="error">{{error}}</p>
      {{/unless}}
    {{else if success}}
      <p class="success">{{success}}</p>
    {{/if}}
  </form>
</section>