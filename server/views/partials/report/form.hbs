<form
  id="report-form"
  hx-post="/api/links/report"
  hx-sync="this:abort"
  hx-swap="outerHTML"
> 
  {{#if message}}
    <p class="success">{{message}}</p>
  {{else}}
    <div class="inputs-wrapper">
      <label>
        {{t "ui.url_containing_malware_scam"}}
        <input 
          type="text" 
          id="link" 
          name="link" 
          placeholder="{{t 'ui.value_example' value1=default_domain}}"
          hx-preserve="true"
          class="{{#if errors.link}}error{{/if}}"
          required 
        />
      </label>
      <button type="submit" class="primary">
        <span>{{> icons/spinner}}</span>
        {{t "ui.send_report"}}
      </button>
    </div>
    {{#if error}}<p class="error">{{error}}</p>{{/if}}
  {{/if}}
</form>