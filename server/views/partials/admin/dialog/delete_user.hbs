<div class="content">
  <h2>{{t "ui.delete_user"}}</h2>
  <p>
    {{t "dialog.delete_user" value=email}}<br/>
    {{t "moderation.delete_user_preserves_links" count=link_count}}
  </p>
  <div class="buttons">
    <button type="button" data-ui-close-dialog>{{t "ui.cancel"}}</button>
    <button 
      type="button"
      class="danger confirm" 
      hx-delete="/api/users/admin/{id}" 
      hx-ext="path-params" 
      hx-vals='{"id":"{{id}}"}' 
      hx-target="closest .content" 
      hx-swap="none" 
      hx-indicator="closest .content"
      hx-select-oob="#dialog-error"
    >
      <span>{{> icons/trash}}</span>
      {{t "ui.delete"}}
    </button>
    {{> icons/spinner}}
  </div>
  <div id="dialog-error">
    {{#if error}}
      <p class="error">{{error}}</p>
    {{/if}}
  </div>
</div>
