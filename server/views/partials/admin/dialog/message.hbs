<div class="content">
  {{#if error}}
    <p>{{error}}</p>
  {{else}}
    <p>{{message}}</p>
  {{/if}}
  <div class="buttons">
    <button type="button" data-ui-close-dialog>{{t "ui.close"}}</button>
  </div>
</div>
