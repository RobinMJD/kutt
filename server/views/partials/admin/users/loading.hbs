{{#unless users}}
  {{#ifEquals users.length 0}}
    <tr class="no-data">
      <td>
        {{t "ui.no_users"}}
      </td>
    </tr>
  {{else}}
    <tr class="loading-placeholder">
      <td>
        {{> icons/spinner}}
        {{t "ui.loading_users"}}
      </td>
    </tr>
  {{/ifEquals}}
{{/unless}}