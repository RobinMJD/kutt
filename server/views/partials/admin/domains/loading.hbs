{{#unless table_domains}}
  {{#ifEquals table_domains.length 0}}
    <tr class="no-data">
      <td>
        {{t "ui.no_domains"}}
      </td>
    </tr>
  {{else}}
    <tr class="loading-placeholder">
      <td>
        {{> icons/spinner}}
        {{t "ui.loading_domains"}}
      </td>
    </tr>
  {{/ifEquals}}
{{/unless}}