<td class="actions">
  <a class="button action" href="/link/tracking/{{id}}" title="Tracking settings" aria-label="Tracking settings">{{> icons/shield}}</a>
  <a class="button action" href="/link/routing/{{id}}" title="Redirect rules" aria-label="Redirect rules">{{> icons/shuffle}}</a>
  <a class="button action" href="/link/forwarding/{{id}}" title="Path and query forwarding" aria-label="Path and query forwarding">{{> icons/chevron_right}}</a>
  <a class="button action" href="/link/health/{{id}}" title="Destination health" aria-label="Destination health">{{> icons/heart}}</a>
  {{#if password}}
    <button class="action password" disabled="true" aria-label="Password protected" data-tooltip="Password protected">
      {{> icons/key}}
    </button>
  {{/if}}
  {{#if banned}}
    <button class="action banned" disabled="true" aria-label="Banned" data-tooltip="Banned">
      {{> icons/stop}}
    </button>
  {{/if}}
  <a
    class="button action stats"
    href="/stats?id={{id}}"
    title="Statistics"
    aria-label="Statistics for {{link.address}}"
  >
    {{> icons/chart}}
  </a>
  <a class="button action qrcode" href="/link/qr/{{id}}" title="QR code" aria-label="QR code">
    {{> icons/qrcode}}
  </a>
  <button 
    class="action edit"
    id="edit-opener-{{id}}"
    type="button"
    aria-label="Edit {{link.address}}"
    title="Edit link"
    hx-trigger="click queue:none"
    hx-ext="path-params"
    hx-get="/link/edit/{id}" 
    hx-vals='{"id":"{{id}}"}'
    hx-swap="beforeend settle:0ms"
    hx-target="next tr.edit"
    hx-indicator="next tr.edit"
    hx-sync="this:drop"
    hx-on::before-request="
      const tr = event.detail.target;
      tr.classList.add('show');
      if (tr.querySelector('.content')) {
        event.preventDefault();
        tr.classList.remove('show');
        tr.removeChild(tr.querySelector('.content'));
      }
    "
  >
    {{> icons/pencil}}
  </button>
  <button 
    class="action delete"
    type="button"
    aria-label="Move {{link.address}} to trash"
    title="Move to trash"
    hx-on:click='openDialog("link-dialog")' 
    hx-get="/confirm-link-delete" 
    hx-target="#link-dialog .content-wrapper" 
    hx-indicator="#link-dialog" 
    hx-vals='{"id":"{{id}}"}'
  >
    {{> icons/trash}}
  </button>
</td>
