<tr id="tr-{{id}}" {{#if swap_oob}}hx-swap-oob="true"{{/if}}>
  <td class="original-url right-fade">
    <span class="table-field-label" aria-hidden="true">Original URL</span>
    <a href="{{target}}" title="{{target}}" target="_blank" rel="noopener noreferrer">
      {{target}}
    </a>
    {{#if description}}
      <p class="description">
        {{description}}
      </p>
    {{/if}}
  </td>
  <td class="created-at">
    <span class="table-field-label" aria-hidden="true">Created at / availability</span>
    {{relative_created_at}}
    <p id="lifecycle-status-{{id}}" class="description">{{lifecycle_status}}</p>
    {{#if relative_expire_in}}
      <p class="description">
        Expires in {{relative_expire_in}}
      </p>
    {{/if}}
  </td>
  <td class="short-link right-fade">
    <span class="table-field-label" aria-hidden="true">Short link</span>
    <div class="clipboard small">
      <button 
        aria-label="Copy" 
        hx-on:click="handleShortURLCopyLink(this);"
        data-url="{{link.url}}"
      >
        {{> icons/copy}}
      </button>
      {{> icons/check}}
    </div>
    <a href="{{link.url}}" target="_blank" rel="noopener noreferrer">
      {{link.link}}
    </a>
  </td>
  <td class="views">
    <span class="table-field-label" aria-hidden="true">Views</span>
    {{visit_count}}
  </td>
    {{> links/actions}}
</tr>
<tr class="edit">
  <td class="loading">
    {{> icons/spinner}}
  </td>
</tr>
