# Remediation captures

Synthetic loopback-only fixtures; no production accounts or tokens. Each linked
capture was opened and visually inspected, not accepted on file existence alone.

`expiry-conflict-{1440,390,320}.png`: exact hardened `.19.2` image, retained new
duration and current expiry after a real independent client's clear. Native form
retry, independent sibling drafts and public paused status were also asserted.
The mobile table clipping/contrast visible outside this fix remain open findings.
These screenshots do not represent a human MFA or physical-phone acceptance.
